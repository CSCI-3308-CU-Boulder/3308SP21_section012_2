import React, { useState } from 'react'
import styles from '../Home/index.module.css'

const Timestamps = () => {
    return(
        <div className={styles.header}>This is the Timestamps Page</div>
    )
}

export default Timestamps;