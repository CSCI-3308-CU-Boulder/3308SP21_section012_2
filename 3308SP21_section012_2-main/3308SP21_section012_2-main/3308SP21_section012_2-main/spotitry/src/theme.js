import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
      primary: {
        main: '#000000',
        light: '#282c34',
      },
      secondary: {
        main: '#1DB954',
      },
    },
  });

export default theme;